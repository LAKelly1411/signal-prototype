# Anthropic (pipeline scoring)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5

# Companies House (pipeline collector — used from Phase 1 onward)
COMPANIES_HOUSE_API_KEY=

# Dashboard (Streamlit Cloud secrets, not read from .env in deployment —
# documented here so every var used anywhere in the project is listed in one place)
DASHBOARD_PASSWORD=
DATA_RAW_URL=https://raw.githubusercontent.com/LAKelly1411/signal-prototype/main/data/signals.json
GITHUB_TOKEN=
